# Azure SQL Database Configuration
SQL_SERVER=tu-servidor.database.windows.net
SQL_DATABASE=SmartUniBotDB
SQL_USER=tu-usuario
SQL_PASSWORD=tu-contraseña-segura

# Azure Functions Configuration
FUNCTIONS_WORKER_RUNTIME=node
AzureWebJobsStorage=UseDevelopmentStorage=true

# Notas:
# 1. Copia este archivo como local.settings.json para desarrollo local
# 2. En Azure, configura estas variables en: Static Web App > Configuration > Application settings
# 3. NUNCA subas local.settings.json con credenciales reales a Git (ya está en .gitignore)
